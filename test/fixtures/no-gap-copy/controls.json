{
  "controls": [{
    "key": "broken", "title": "Broken", "category": "Test", "sortOrder": 0,
    "question": "Yes?",
    "answer": { "type": "enum", "options": [{ "value": "y", "label": "Yes" }] },
    "rules": [
      { "when": { "op": "eq", "value": "y" }, "status": "pass" },
      { "when": { "op": "always" }, "status": "fail" }
    ],
    "gap": {}
  }]
}
